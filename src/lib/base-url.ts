// Base URL for the application
// In production on Cloudflare, this will be set by the platform
// For GitHub Pages, we use the repository name as the base path
export const baseUrl = import.meta.env.BASE_URL?.replace(/\/$/, '') || '/theundertakerpart2.github.io';

